services:
  frontend:
    build:
      context: .
      dockerfile: docker/web/Dockerfile
    container_name: luma-frontend
    ports:
      - "4005:4200"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    stdin_open: true
    tty: true
