import 'package:flutter/material.dart';

abstract final class AppColors {
  // Brand
  static const primary = Color(0xFF5FD88A);
  static const primaryDark = Color(0xFF2F9D68);
  static const primaryLight = Color(0xFF8BE7A8);

  // Backgrounds
  static const background = Color(0xFF031411);
  static const backgroundSoft = Color(0xFF081B17);
  static const surface = Color(0xFF0B2420);
  static const surfaceLight = Color(0xFF12342D);

  // Text
  static const text = Color(0xFFF5F7F6);
  static const textMuted = Color(0xFFA7BBB5);
  static const textSubtle = Color(0xFF718B83);

  // Borders / dividers
  static const border = Color(0xFF1C3933);
  static const divider = Color(0xFF19342E);

  // Semantic
  static const success = Color(0xFF67D995);
  static const error = Color(0xFFFF756B);
  static const warning = Color(0xFFF4C95D);
  static const info = Color(0xFF70B7D8);

  // Financial
  static const income = Color(0xFF67D995);
  static const expense = Color(0xFFFF756B);
  static const balance = Color(0xFFF5F7F6);

  // Neutral / icons
  static const icon = Color(0xFFB8C9C4);
  static const iconMuted = Color(0xFF718B83);
  static const disabled = Color(0xFF3B514B);

  // Overlay
  static const overlay = Color(0x66000000);
}
